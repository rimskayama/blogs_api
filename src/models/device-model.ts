export type deviceInputModel = {
    userId: string,
    IP: string,
    title: string,
    lastActiveDate: string,
    deviceId: string,
    expDate: string
}

export type deviceViewModel = {
    IP: string,
    title: string,
    lastActiveDate: string,
    deviceId: string,
}